[project]
name = "a-ponte"
version = "1.0.0"
description = "Servidor MCP da Central de Salas e o agente A2A que o consome por dentro."
requires-python = ">=3.10"
dependencies = [
    "mcp==2.2.0",
    "starlette==1.7.0",
    "uvicorn==0.53.0",
]

[tool.uv]
package = false
